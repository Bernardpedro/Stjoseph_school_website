# Local XAMPP (npm run dev). Vite proxies /api to the backend.
# NUXT_PUBLIC_API_BASE=http://localhost:3000
NUXT_PUBLIC_DEFAULT_LOCALE=en

# Production build (npm run generate) uses .env.production instead:
NUXT_PUBLIC_API_BASE=https://api.stjosephtssnzuki.com
